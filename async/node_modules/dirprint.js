var fs  = require('fs');


var dirprint = function(dirloc) {

	var writeStream = fs.createWriteStream('./log.txt', 
                        {
                            'flags': 'a',
                            'encoding': 'utf8',
                            'mode': 0666
                        }
    );


    try {

        fs.readdir(dirloc, function(err, files) {
            files.forEach(function(name) {
                // print file name
                writeStream.write('Found file: ' + name +'\n', function(err) {
                    console.error(err);
                });
            });

            
        });

    } catch (err) {
    	console.error(util.inspect(err));
    }

};// end print()

exports.dirprint = dirprint;
